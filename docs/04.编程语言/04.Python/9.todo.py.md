# todo.py 代码

```python
# ─────────────────────────────────────────────
#  命令行任务管理器（综合实战）
#  覆盖：class/函数/装饰器/生成器/异常/文件/argparse
#  运行：python todo.py add "买牛奶"
#        python todo.py list
#        python todo.py done 1
#        python todo.py remove 1
# ─────────────────────────────────────────────
import json
from pathlib import Path
import argparse
from datetime import datetime

DATA_FILE = Path(__file__).parent / "tasks.json"


class Task:
    """单个任务"""

    def __init__(self, id, title, done=False):
        self.id = id
        self.title = title
        self.done = done

    def to_dict(self):
        """转成字典（方便存 JSON）"""
        return {"id": self.id, "title": self.title, "done": self.done}

    @classmethod
    def from_dict(cls, data):
        """从字典创建 Task"""
        return cls(data["id"], data["title"], data["done"])

    def __str__(self):
        status = "[x]" if self.done else "[ ]"
        return f"{status} {self.id}. {self.title}"


def log(func):
    """装饰器：记录操作日志"""
    def wrapper(*args, **kwargs):
        result = func(*args, **kwargs)
        print(f"[{datetime.now():%H:%M}] {func.__name__} 完成")
        return result
    return wrapper


class TaskManager:
    """任务管理器：负责增删改查 + 持久化"""

    def __init__(self):
        self.tasks = []
        self._load()

    # ── 文件持久化 ──────────────────────────
    def _load(self):
        """从文件加载任务"""
        if DATA_FILE.exists():
            with open(DATA_FILE, encoding="utf-8") as f:
                data = json.load(f)
                self.tasks = [Task.from_dict(t) for t in data]

    def _save(self):
        """保存任务到文件"""
        with open(DATA_FILE, "w", encoding="utf-8") as f:
            json.dump([t.to_dict() for t in self.tasks], f, ensure_ascii=False, indent=2)

    # ── 核心操作 ────────────────────────────
    @log
    def add(self, title):
        """新增任务"""
        new_id = (self.tasks[-1].id + 1) if self.tasks else 1
        self.tasks.append(Task(new_id, title))
        self._save()

    @log
    def remove(self, task_id):
        """删除任务"""
        # 生成器：过滤掉指定 id
        self.tasks = [t for t in self.tasks if t.id != task_id]
        self._save()

    @log
    def done(self, task_id):
        """标记完成"""
        for t in self.tasks:
            if t.id == task_id:
                t.done = True
                break
        self._save()

    def show(self):
        """显示所有任务"""
        if not self.tasks:
            print("暂无任务")
            return

        # lambda + sorted：按 id 排序
        for t in sorted(self.tasks, key=lambda t: t.id):
            print(t)

        # 统计（生成器 + sum）
        total = len(self.tasks)
        finished = sum(1 for t in self.tasks if t.done)
        print(f"\n共 {total} 项，已完成 {finished} 项")


def main():
    parser = argparse.ArgumentParser(description="命令行任务管理器")
    sub = parser.add_subparsers(dest="command", required=True)

    sub.add_parser("list", help="查看所有任务")
    p_add = sub.add_parser("add", help="新增任务")
    p_add.add_argument("title", help="任务内容")
    p_done = sub.add_parser("done", help="标记完成")
    p_done.add_argument("id", type=int, help="任务 ID")
    p_rm = sub.add_parser("remove", help="删除任务")
    p_rm.add_argument("id", type=int, help="任务 ID")

    args = parser.parse_args()
    mgr = TaskManager()

    try:
        if args.command == "list":
            mgr.show()
        elif args.command == "add":
            mgr.add(args.title)
            mgr.show()
        elif args.command == "done":
            mgr.done(args.id)
            mgr.show()
        elif args.command == "remove":
            mgr.remove(args.id)
            mgr.show()
    except Exception as e:
        print(f"操作失败：{e}")


if __name__ == "__main__":
    main()

```
