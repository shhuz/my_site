# 步骤 7：自定义命令与生成文件

代码生成是扩展编程语言超出其语言模型范围的普遍机制。CMake 对 Qt 的元对象编译器有一流支持，但很少有其他的代码生成器值得这种程度的努力。

相反，代码生成器往往是定制和特定用途的。CMake 提供了描述代码生成器使用的设施，以便项目可以为其个别需求添加支持。

在本节中，我们将使用 `add_custom_command()` 在教程项目中添加对代码生成器的支持。

## 背景

构建过程中的任何步骤通常都可以用其输入和输出来描述。CMake 假设代码生成器和其他自定义过程遵循相同的原则。通过这种方式，代码生成器与编译器、链接器以及工具链的其他元素完全相同；当输入比输出更新（或输出不存在）时，将运行用户指定的命令来更新输出。

> **注意** 此模型假设进程的输出在运行之前是已知的。CMake 缺乏描述输出名称和位置取决于输入内容的代码生成器的能力。存在各种技巧可以将此功能移植到 CMake，但它们不在本教程的范围内。
>
> 描述代码生成器（或任何自定义过程）通常在两个部分中执行。首先，输入和输出是独立于 CMake 目标模型描述的，只关注生成过程本身。其次，输出与 CMake 目标关联以将它们插入 CMake 目标模型。

对于源文件，这就像将生成的文件添加到 STATIC、SHARED 或 OBJECT 库的源列表一样简单。对于纯头文件生成器，通常需要使用通过 `add_custom_target()` 创建的中继目标将头文件生成添加到构建阶段（因为 INTERFACE 库没有构建步骤）。

## 练习 1 - 使用代码生成器

描述代码生成器的主要机制是 `add_custom_command()` 命令。就 `add_custom_command()` 而言，"命令"要么是构建环境中可用的可执行文件，要么是 CMake 可执行目标名称。

```cmake
add_executable(Tool)
# ...
add_custom_command(
  OUTPUT Generated.cxx
  COMMAND Tool -i input.txt -o Generated.cxx
  DEPENDS Tool input.txt
  VERBATIM
)
# ...
add_library(GeneratedObject OBJECT)
target_sources(GeneratedObject
  PRIVATE
    Generated.cxx
)

大多数关键字是自解释的，除了 VERBATIM。此参数从技术上讲是强制性的，因为遗留原因在现代上下文中解释无趣。好奇者可以查阅 `add_custom_command()` 文档以获取更多详细信息。

Tool 可执行目标同时出现在 COMMAND 和 DEPENDS 参数中。虽然 COMMAND 足以使代码正确构建，但将 Tool 本身作为自定义命令的依赖项确保如果 Tool 更新，自定义命令将重新运行。

对于纯头文件生成，需要额外的命令，因为库本身没有构建步骤。我们可以使用 `add_custom_target()` 为库创建"人工"构建步骤。然后我们使用 `add_dependencies()` 命令强制自定义目标在任何链接该库的目标之前运行。

```cmake
add_custom_target(RunGenerator DEPENDS Generated.h)

add_library(GeneratedLib INTERFACE)
target_sources(GeneratedLib
  INTERFACE
    FILE_SET HEADERS
    BASE_DIRS
      ${CMAKE_CURRENT_BINARY_DIR}
    FILES
      ${CMAKE_CURRENT_BINARY_DIR}/Generated.h
)

add_dependencies(GeneratedLib RunGenerator)
```

> **注意** 我们添加了 CMAKE_CURRENT_BINARY_DIR，这是一个命名我们的产物被放置的构建树当前位置的变量，到基础目录，因为那是我们的代码生成器将在其中运行的工作目录。列出 FILES 对于构建来说是不必要的，这里只做以便理解。

### 目标

将预计算平方根表添加到 MathFunctions 库。

### 帮助资源

- add_executable()
- add_library()
- target_sources()
- add_custom_command()
- add_custom_target()
- add_dependencies()

### 待编辑文件

MathFunctions/CMakeLists.txt
MathFunctions/MakeTable/CMakeLists.txt
MathFunctions/MathFunctions.cxx

### 入门

MathFunctions 库已被编辑为在给定小于 10 的数字时使用预计算表。然而，硬编码的表并不是特别准确，仅包含最接近的截断整数值。

MakeTable.cxx 源文件描述了一个将生成更好表的程序。它接受单个参数作为输入，即要生成的表文件名。

完成 TODO 1 到 TODO 10。

### 构建和运行

无需特殊配置，像往常一样配置和构建。注意 MakeTable 可执行文件在 MathFunctions 之前排序。

```bash
cmake --preset tutorial
cmake --build build
```

验证 Tutorial 的输出现在对小于 10 的值使用预计算表。

### 解决方案

首先我们添加一个新可执行文件来生成表，将 MakeTable.cxx 文件作为源。

**TODO 1-2：点击显示/隐藏答案**

然后我们添加一个生成表的自定义命令，以及依赖表的自定义目标。

**TODO 3-4：点击显示/隐藏答案**

我们需要添加一个接口库来描述将出现在 CMAKE_CURRENT_BINARY_DIR 中的输出。FILES 参数是可选的。

**TODO 5-6：点击显示/隐藏答案**

现在所有目标都已描述，我们可以通过将它们与 add_dependencies() 关联来强制自定义目标在接口库的任何依赖项之前运行。

**TODO 7：点击显示/隐藏答案**

我们已经准备好将接口库添加到 MathFunctions 的链接库中，并将整个 MakeTable 文件夹添加到项目中。

**TODO 8-9：点击显示/隐藏答案**

最后，我们更新 MathFunctions 库本身以利用生成的表。

**TODO 10：点击显示/隐藏答案**
```